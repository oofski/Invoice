# Build-time default for the Cloudflare Worker REST API base URL.
# The installed app can override this at runtime via the login screen's
# "Server URL" field (stored in localStorage "iq_api_base"), so a rebuild is
# NOT required to point the app at a different Worker.
VITE_API_BASE_URL=https://invoiceiq.acct.workers.dev
